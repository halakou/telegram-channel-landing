{
  "name": "telegram-channel-landing",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "wrangler dev",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "@astrojs/tailwind": "^6.0.0",
    "@types/qrcode": "^1.5.5",
    "autoprefixer": "^10.4.20",
    "tailwindcss": "^3.4.0",
    "vitest": "^3.0.0",
    "wrangler": "^3.99.0"
  }
}
